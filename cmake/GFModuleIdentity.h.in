/**
 * Generated by gf_add_module() from this module's module.json. Do not edit.
 *
 * Deliberately minimal: only what the runtime actually cross-checks or a macro
 * actually expands. A module's name, description and author are NOT here --
 * they come from the signed package manifest, which is the better source
 * because the host has verified a signature over it. Compiling a second copy
 * into the binary would create a second source of truth that nothing reconciles.
 */

#pragma once

#define GF_MODULE_ID "@GF_MODULE_ID@"
#define GF_MODULE_VERSION "@GF_MODULE_VERSION@"
#define GF_MODULE_TRANSLATION_CONTEXT "@GF_MODULE_TRANSLATION_CONTEXT@"
